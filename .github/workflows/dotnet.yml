name: .NET

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v6
    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: '10.0.x'
    - name: Install dependencies
      run: dotnet restore Fp.slnx
    - name: Build
      run: dotnet build Fp.slnx --configuration Release --no-restore
    - name: Test
      run: dotnet test Fp.slnx --configuration Release --no-build --verbosity normal
